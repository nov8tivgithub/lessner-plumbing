RewriteEngine On

# If the request is not for an existing file or directory,
# route it through index.php (CodeIgniter front controller).
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [L]
